To test select activities implementation, run `python select_activities.py`.
Script output will be saved to a file named `actions.out`.
The script assumes there is an `act.txt` file in the same directory.